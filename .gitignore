# 构建产物
build/
/server
/fileupload

# 运行时数据
/storage/

# 编辑器
.idea/
.vscode/
*.swp
*.swo
*~

# 系统
.DS_Store
Thumbs.db

# Claude Code session data
.claude/

# 环境
.env
.env.local
*.exe

# 运行时配置文件（含敏感信息，参见 fileupload.yaml.example）
fileupload.yaml

# 前端
web/node_modules/
# web/dist: 全部为构建产物（本地 make web / CI 的 web job 重新生成）。
# 服务端通过 web/embed.go 内嵌 web/dist，因此 Go 编译前必须先备好该目录：
#   - 本地：make server 依赖 web target 先构建前端
#   - CI backend job：放置占位产物即可通过编译
#   - CI build job（server）：下载 web job 产出的 web-dist artifact
web/dist/

# 测试覆盖率报告（生成产物，可由 scripts/coverage.sh 重新生成）
web/coverage/
web/coverage-report.txt
go-coverage-report.txt

# JS SDK（v0.5.0+）
sdk/js/node_modules/
sdk/js/dist/

# JS 包管理器缓存与 vitest 缓存
node_modules/
.pnpm-store/
.vite/

# 本地工具缓存
.cache/
.uv-cache/
.test-home/
.gocache/

# 浏览器测试产物
web/playwright-report/
web/test-results/
/release/
