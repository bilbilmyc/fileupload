# 构建产物
build/
/server
/fileupload

# 运行时数据
/storage/

# 编辑器
.idea/
.vscode/
*.swp
*.swo
*~

# 系统
.DS_Store
Thumbs.db

# Claude Code session data
.claude/

# 环境
.env
.env.local
*.exe

# 运行时配置文件（含敏感信息，参见 fileupload.yaml.example）
fileupload.yaml

# 前端
web/node_modules/
# web/dist: 全部为构建产物（CI / 本地 make web 都重新生成）。
# 无 Go embed 依赖（embed 检查无果），所以 index.html / placeholder
# 占位文件不需要跟踪 — 全部由 Makefile 的 web target 生成。
web/dist/

# 测试覆盖率报告（生成产物，可由 scripts/coverage.sh 重新生成）
web/coverage/
web/coverage-report.txt
go-coverage-report.txt

# JS SDK（v0.5.0+）
sdk/js/node_modules/
sdk/js/dist/

# 根目录 node_modules（如果以后有顶层 npm 依赖）+ vitest 缓存
node_modules/
.vite/

# 本地工具缓存
.cache/
.uv-cache/
.test-home/
